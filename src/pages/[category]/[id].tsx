import React from 'react';
import DetailPage from '../../components/DetailPage';

const DetailPageWrapper = ({ theme }: { theme: string }) => {
  return <DetailPage theme={theme} />;
};

export default DetailPageWrapper;